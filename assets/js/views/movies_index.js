'use strict';

(() => {
    // function startTimer () {
    //     timer.start();
    //     setTimeout(stopTimer,5000);
    // }

    // function stopTimer () {
    //     timer.stop();
    // }

    // $.ajax({
    //     url: 'api/movies/locations',
    //     type: 'GET',
    //     success: function(data) {
    //         const map = document.getElementById('map');
    //         var i, j, temparray, chunk = 50;
    //         for (let i = 0, j = data.locations.length; i < j; i += chunk) {
    //             temparray = data.locations.slice(i, i + chunk);
    //             for (let i = 0; i < temparray.length; i++) {
    //                 googlemaps.renderGoogleMap(temparray);
    //             }
    //         }
    //     },
    // });
})();
